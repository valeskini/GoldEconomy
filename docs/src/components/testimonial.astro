---
interface Props {
	date: string;
	name: string;
	stars: string;
	cite: string;
}

const { name, date , cite } = Astro.props;
---

<li class="testimonial">
	<blockquote class="quote" cite={cite}>
		<slot />
	</blockquote>
	<div class="footer">
		<div>
			<p class="name">{name}</p>
			<a href={cite} class="handle">{date}</a>
		</div>
	</div>
</li>

<style>
	.testimonial {
		display: flex;
		flex-direction: column;
		gap: 0.5em;
	}
	@media (min-width: 50rem) {
		.testimonial {
			gap: 0.5em;
		}
	}
	.quote {
		position: relative;
		padding-inline-start: 1.5em;
	}
	.quote::before {
		position: absolute;
		content: '';
		inset-block: 0.5em;
		inset-inline-start: 0;
		border-inline-start: 1px solid var(--sl-color-text-accent);
	}
	.quote > :global(* + *) {
		margin-top: 0.75em;
	}
	.footer {
		display: flex;
		gap: 1rem;
		align-items: center;
		justify-content: center;
	}
	.name {
		font-weight: 600;
		font-size: var(--sl-text-h4);
		color: var(--sl-color-white);
		line-height: var(--sl-line-height-headings);
	}
	.handle {
		text-underline-offset: 4px;
		color: var(--sl-color-text-accent);
	}
	.handle:hover {
		color: var(--sl-color-white);
	}
</style>